Testing with PHPUnit (http://www.phpunit.de/)

To run an individual test:

    phpunit SpecificTest.php

To run all tests it is best to execute the Phing build script
which has the appropriate filters setup to ensure that only
the tests that need to be run will be run.

    phing test
